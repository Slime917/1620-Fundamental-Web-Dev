git config --global user.name "Nathan Siu"
git config --global user.email "Nathan siu1310257@gmail.com"
git config --global core.editor "code --wait"

git config --global --get core.editor
git config --global --get init.defualtBranch main (set default branch to main)

mkdir folder_name
git init (to initalize repo)
git branch

git config --gloabl --list
git config --local --list --show-origin | Select-String -Pattern 'core\.editor|color\.iu'
git config --system --list

git help -a
git help tutorital
git help

Get-childItem -Force (see hiden .git folders in cmd)

echo "# My Notes" (contents inside file) > READMW.md (file) >> to append > to over write

git add . (to add all)
git add File_name (to add specific file)

git commit -m (the branch)

git commit --origin
git remote add origin https://github.com/<USERNAME>/<REPO>.git
git push -u origin main

git clone

git branch Branch_name (to create a new branch)
git checkout Branch_name (to switch branch)
git switch -c Branch_name (create and switch to branch)


Add-content. gitignore ".log"

untracked
unmodified
modified
staging

git remote -v (check fetch and push origins)
git push -u origin new_branch_name (for upstream)

git status (show files whether they are in one of the four status)

git branch (shows branch and stars current working branch)

get diff (shows staged and not staged files and lines)
git diff --staged
git diff --name-only (file only)
git diff --stat (shows files and instructions modified)
git log --oneline -n 2 (shows log and how many lines)

git restore --staged file_name (unstages)
commit -a to commit non staged or modified files but hidden
git status -s (shows branch)

remove-Item file_name (only locally)
git mv file_name Newfolder_name